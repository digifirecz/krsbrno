node_modules/
.next/
coverage/
.DS_Store
*.log
.env*
!.env.example
tsconfig.tsbuildinfo
.idea/

# Uploads now live in Supabase Storage — these only ever fill up with local
# dev-test files, never real content the app actually serves.
public/image/
public/audio/
